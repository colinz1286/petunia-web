'use client';

// NOTE: This web page is intended to mirror the iOS view at
// .local-only/ios-real-reference/BreederCurrentLittersView.swift.
// Keep litter list behavior and related actions aligned across both files.
export default function BreederCurrentLittersPage() {
  return (
    <main className="min-h-screen bg-[color:var(--color-background)] text-[color:var(--color-foreground)] flex items-center justify-center">
      <h1 className="text-2xl font-bold text-[color:var(--color-accent)]">
        page coming soon!
      </h1>
    </main>
  );
}
